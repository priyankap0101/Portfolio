1. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
2. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
3. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
4. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
5. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
6. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
7. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
8. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
9. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
10. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
11. **You**: Check the following code for bugs and provide repair suggestions: 
```css
/* Global styles */
body {
  font-family: "Playfair Display", serif; /* Replace 'Poppins' with your chosen font */
  margin: 0;
  padding: 0;
  overflow-x: hidden;
}

/* Add a black overlay */
body::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;

  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with 50% opacity */
}

/* Text at the top left corner */
.name {
  position: absolute;
  top: 20px;
  left: 20px;
  color: #fff; /* Text color */
  font-size: 20px; /* Font size */
  font-weight: bold; /* Font weight */
  z-index: 9999; /* Ensure it's above other content */
}

/* Menu Icon styles */
.menu-icon {
  position: fixed;
  top: 20px;
  right: 20px;
  z-index: 9999;
  cursor: pointer;
}

.bar {
  width: 30px;
  height: 3px;
  background-color: #fff;
  margin: 6px 0;
  transition: 0.4s;
}

/* Menu Icon open state */
.menu-icon.open .bar:nth-child(1) {
  transform: rotate(-45deg) translate(-5px, 7px);
}

.menu-icon.open .bar:nth-child(2) {
  opacity: 0;
}

.menu-icon.open .bar:nth-child(3) {
  transform: rotate(45deg) translate(-5px, -7px);
}

/* Full-screen overlay */
.overlay {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgb(3, 24, 60); /* Semi-transparent black overlay */
  z-index: 9998;
  display: none; /* Initially hidden */
}

.overlay.open {
  display: block; /* Display when menu is open */
}

/* Navigation Links */
.nav-links {
  position: fixed;
  top: 13%;
  left: 50%; /* Center horizontally */
  transform: translate(-50%, -50%);
  text-align: center;
  width: 100%;
  z-index: 9999; /* Ensure links are above overlay */
  display: none; /* Initially hidden */
}

.nav-links.open {
  display: block; /* Display when menu is open */
}

.cancel-icon {
  position: absolute;
  top: 20px;
  right: 20px;
  color: #fff;
  font-size: 24px;
  cursor: pointer;
}

/* Navigation Link styles */
.nav-link {
  display: block;
  font-size: 4rem; /* Increased font size */
  font-weight: bolder; /* Bold font weight */
  color: #888; /* Grey color */
  padding: 20px;
  text-decoration: none;
  transition: color 0.2s; /* Only transition the color property */
  cursor: pointer; /* Pointer cursor */
}

.nav-link:hover {
  color: #fff; /* White text color on hover */
}

/* Home Section */
#home {
  background-image: url("./img/bg18.jpg");
  background-repeat: no-repeat;
  background-size: cover;
  position: relative;
  height: 100vh;
  display: flex;
  justify-content: center;
  align-items: center;
  text-align: center;
  filter: blur();
  -webkit-filter: blur();
}
#home::before {
  content: "";
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  background-color: rgba(0, 0, 0, 0.5); /* Black overlay with opacity */
  z-index: -1; /* Place the overlay behind the text content */
}

.container {
  width: 80%;
  margin: 0 auto;
}

h1.aos-init.aos-animate {
  color: whitesmoke !important;
  margin-top: 2rem !important;
}

.arrow-icon {
  display: block;
  font-size: 2rem;
  margin-top: -40px;
  color: #58cfed;
}

.home-content {
  position: relative;
  z-index: 1;
  max-width: 800px;
}

p {
  font-size: 15px;
  font-weight: bolder;
  margin-bottom: 40px;
}

.home-section h1 {
  margin-top: 9rem;
  font-size: 3.7rem;
  line-height: 1.3;
  color: #fffafa;
}

/* Blur effect when menu is open */
body.nav-open {
  backdrop-filter: blur(10px); /* Adjust the blur radius as needed */
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for all sections with class "next-section" */
.next-section {
  padding: 150px !important;
  background-color: #0a1f46;
  color: #fff;
}

/* CSS for the container within each section */
.next-section .container {
  text-align: center;
}

/* CSS for the flex container within each section */
.next-section .flex-container {
  display: flex;
  justify-content: center;
  align-items: center;
  flex-wrap: wrap; /* Ensure items wrap to new line if needed */
}

/* CSS for each paragraph container within each section */
.next-section .paragraph-container {
  width: calc(25% - 20px); /* Adjust width as needed */
  margin: 10px; /* Adjust margin between containers */
}

/* CSS for each paragraph within each section */
.next-section .paragraph {
  font-size: 18px;
  color: #fff;
  margin-top: 10px;
}

/* CSS for the icon within each paragraph */
.next-section .paragraph i {
  margin-bottom: 10px; /* Add spacing between icon and text */
}
.picon {
  font-size: 5rem;
}
/* CSS for the additional text container */
.additional-text-container {
  display: flex;
  justify-content: space-between; /* Ensure even spacing between items */
  align-items: center;
  flex-wrap: wrap; /* Allow wrapping of content */
  color: #fff; /* Text color */
  text-align: center;
}

/* CSS for the intro text */
.intro-text {
  font-size: 2.3rem;
  font-weight: bolder; /* Keeping font weight */
  width: 24rem;
  text-align: right; /* Align text to the right */
}

/* CSS for the passionate text */
.passionate-text {
  font-size: 18px;
  margin-bottom: 20px;
  flex: 1; /* Take up available space */
  text-align: left; /* Align text to the left */
  margin-left: 1.5rem;
}


/* CSS for the information section */
.information-section {
  padding: 0px 0;
}

/* CSS for the container */
.infocontainer {
  /* display: flex; */
  /* justify-content: space-around; */
  /* background: linear-gradient(to right, #faf0e6 50%, #040b5a 50%); */
}

/* CSS for the circle containers */

/* CSS for the circle containers */
.circle-container {
  display: inline-block;
  margin: 88px; /* Adjust margin as needed */
}

/* CSS for the circles */
.circle {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  width: 18rem; /* Adjust width as needed */
  height: 16rem; /* Adjust height as needed */
  border: 8px solid transparent; /* Add initial border */
  border-radius: 50%; /* Make the circle */
  transition: border-radius 0.3s ease; /* Transition for border-radius */
}

/* CSS for the blue circle */
.blue-circle {
  color: #fff; /* Text color */
  border-color: #faf0e6; /* Blue border color */
}

/* CSS for the cream circle */
.cream-circle {
  color: #333; /* Text color */
  border-color: #040b5a; /* Cream border color */
}

/* CSS for circle text */
.circle h2,
.circle p {
  margin: 0;
}

/* Hover effect for circles */
.circle:hover {
  border-radius: 5px; /* Make the circle square upon hover */
}
/* CSS for the anchor link with class "infolink" */
a.infolink {
  color: #040b5a; /* Text color */
  text-decoration: none; /* Remove underline */
  font-size: 3rem;
  font-weight: 700;
  padding: 10px 20px; /* Add padding */
  border-radius: 5px; /* Add rounded corners */
  transition: background-color 0.3s ease; /* Smooth transition for background color */
}
a.infolink2 {
  color: #fceddd;
  text-decoration: none;
  font-weight: 500;
  font-size: 3rem;
  padding: 10px 20px;
  border-radius: 5px;
}
/* CSS for the h2 element with class "infotext" */
h2.infotext1 {
  color: #040b5a;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}
h2.infotext2 {
  color: #fceddd;
  font-size: 1rem;
  font-style: italic; /* Set font style to italic */
}

/* Media query for smaller screens */
@media screen and (max-width: 768px) {
  .infocontainer {
    flex-direction: column; /* Stack circles vertically on smaller screens */
  }
  
  .circle-container {
    flex: 1 1 auto; /* Allow circle containers to take full width */
    margin: 10px 0; /* Adjust margin for smaller screens */
  }

  .circle {
    
      height: 11rem;
      width: 11rem;
     
  }
  .blue-circle {
    margin-top: -14rem;
    margin-left: 20rem;
  }
  .cream-circle {
    margin-left: 1rem;
  }
}
/* CSS for the contact form */
.contact-form {
  background: linear-gradient(135deg, #1e3c72, #1d3969);
  padding: 40px;
  border-radius: 20px;
  box-shadow: 0 10px 20px rgba(0, 0, 0, 0.1);
  font-family: "Arial", sans-serif;
  width: 80%;
  max-width: 500px;
  margin: 20px auto;
  color: #fff; /* Text color */
  transition: background-color 0.3s ease;
}

.contact-form:hover {
  background: linear-gradient(135deg, #051e47, #0a1d3f);
}

.contactheading {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

.form-group {
  position: relative;
  margin-bottom: 20px;
}

.form-group label {
  position: absolute;
  top: 10px;
  left: 20px;
  font-size: 16px;
  color: #fff; /* Label color */
  transition: top 0.3s ease, font-size 0.3s ease, color 0.3s ease;
}

.form-group input[type="text"],
.form-group input[type="email"],
.form-group textarea {
  width: 100%;
  height: 2rem;
  padding: 15px;
  border: none;
  border-bottom: 2px solid #fff; /* Input border color */
  background-color: transparent;
  font-size: 16px;
  color: #fff; /* Input text color */
  transition: border-color 0.3s ease;
}

.form-group input[type="text"]:focus,
.form-group input[type="email"]:focus,
.form-group textarea:focus {
  border-color: #ecf0f1; /* Border color on focus */
  outline: none;
}

.form-group input[type="text"]:focus + label,
.form-group input[type="email"]:focus + label,
.form-group textarea:focus + label {
  top: -20px;
  font-size: 14px;
  color: #fff; /* Label color on focus */
}

/* CSS for the button */
button[type="submit"] {
  padding: 15px;
  background: linear-gradient(135deg, #4e73df, #224abe);
  /*Gradientbackground*/
  color: #fff;
  border: none;
  border-radius: 30px;
  font-size: 18px;
  cursor: pointer;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  transition: background-color 0.3s, transform 0.2s;

  -webkit-border-radius: 30px;
  -moz-border-radius: 30px;
  -ms-border-radius: 30px;
  -o-border-radius: 30px;
}

button[type="submit"]:hover {
  background: linear-gradient(
    135deg,
    #224abe,
    #4e73df
  ); /* Gradient background on hover */
  transform: translateY(-2px); /* Move button slightly up on hover */
}

/* Placeholder color */
.contact-form input[type="text"]::placeholder,
.contact-form input[type="email"]::placeholder,
.contact-form textarea::placeholder {
  color: #ccc; /* Placeholder color */
}
/* Style for filled input fields */
/* Style for filled input fields */
.contact-form input[type="text"]:valid,
.contact-form input[type="email"]:valid,
.contact-form textarea:valid {
  background-color: none; /* Remove background color */
  border-color: #007bff; /* Change border color */
}
/* Style for autofilled input fields */
.contact-form input:-webkit-autofill {
  background-color: transparent !important; /* Remove background color */
  border-color: #007bff !important; /* Change border color */
}

/* CSS for the footer */
footer {
  background-color: #000000; /* Footer background color */
  color: #fff; /* Text color */
  padding: 20px;
  display: flex;
  justify-content: space-between;
  align-items: center;
}

/* CSS for the left side of the footer */
.footer-left p {
  margin: 0;
}

/* CSS for the right side of the footer */
.footer-right a {
  color: grey; /* Icon color */
  text-decoration: none;
  margin-left: 10px; /* Adjust spacing between icons */
  transition: color 0.3s ease; /* Smooth transition for icon color */
}

/* Hover effect for footer icons */
.footer-right a:hover {
  color: #fff; /* Change icon color on hover to white */
}

/* CSS for the back-to-top arrow */
#back-to-top {
  font-size: 24px;
}

/* Additional styling for back-to-top arrow */
footer p {
  font-size: small;
  color: grey;
}
/* Add a fade-in transition effect */
.fade-in {
  opacity: 0;
  transition: opacity 0.5s ease-in-out;
}

.fade-in.active {
  opacity: 1;
}

/* Media Queries for responsiveness */

/* Navigation Links */
@media screen and (max-width: 768px) {
  .nav-links {
    top: 25%;
  }
}

@media screen and (max-width: 576px) {
  .nav-links {
    top: 30%;
  }
}

@media screen and (max-width: 480px) {
  .nav-links {
    top: 35%;
  }
}

/* Home Section */
@media screen and (max-width: 768px) {
  #home .home-content h1 {
    font-size: 3rem;
  }
  /* Adjust position for smaller screens */
  .menu-icon {
    top: 15px;
    right: 15px;
  }
}

@media screen and (max-width: 576px) {
  #home .home-content h1 {
    font-size: 2.5rem;
  }
    /* Further adjust position for even smaller screens */
    .menu-icon {
      top: 10px;
      right: 10px;
    }
  }


@media screen and (max-width: 480px) {
  #home .home-content h1 {
    font-size: 2rem;
  }
   /* Adjust size for the smallest screens */
   .menu-icon {
    /* top: 10px;
    right: 10px; */
    /* margin: 7rem; */
    margin-top: auto;
  }
  
  /* Adjust bar size */
  .bar {
    width: 20px;
    height: 2px;
    margin: 4px 0;
  }
  .nav-links {
    margin-top:-51rem;
  }
}

/* Next Section */
@media screen and (max-width: 768px) {
  .next-section .paragraph-container {
    width: calc(50% - 20px);
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph-container {
    width: calc(100% - 20px);
    margin: 10px 0;
  }
}

@media screen and (max-width: 768px) {
  .next-section .paragraph {
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .next-section .paragraph {
    font-size: 14px;
  }
}

@media screen and (max-width: 768px) {
  .additional-text-container {
    flex-direction: column;
  }

  .additional-text-container .intro-text,
  .additional-text-container .passionate-text {
    text-align: center;
    margin-left: -132px;;
    margin-top: 20px;
  }
}

/* Information Section */
@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle-container {
    margin: 20px;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle-container {
    margin: 20px 0;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle h2 {
    font-size: 1.2rem;
  }
}

@media screen and (max-width: 768px) {
  .information-section .infocontainer .circle a {
    font-size: 1.5rem;
  }
}

@media screen and (max-width: 576px) {
  .information-section .infocontainer .circle a {
    font-size: 1.3rem;
  }
}

@media screen and (max-width: 480px) {
  .information-section .infocontainer .circle a {
    font-size: 1.2rem;
  }
}

/* Contact Section */
@media screen and (max-width: 768px) {
  .contact-section .contactheading {
    font-size: 3rem;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contactheading {
    font-size: 2.5rem;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contactheading {
    font-size: 2rem;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .contact-form {
    width: 90%;
    max-width: none;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .contact-form {
    width: 60%;
    max-width: none;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group label {
    font-size: 14px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group label {
    font-size: 12px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 40px;
    padding: 10px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section .form-group input[type="text"],
  .contact-section .form-group input[type="email"],
  .contact-section .form-group textarea {
    height: 36px;
    padding: 8px;
  }
}

@media screen and (max-width: 768px) {
  .contact-section button[type="submit"] {
    padding: 12px;
    font-size: 16px;
  }
}

@media screen and (max-width: 576px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}

@media screen and (max-width: 480px) {
  .contact-section button[type="submit"] {
    padding: 10px;
    font-size: 14px;
  }
}


/* CSS for projects section */
.projects-section {
  padding: 20px;
  /* background-color: #0a1f46; */
  color: #faf0e6;
  text-align: center;
}

.projects-section h2 {
  font-weight: bold;
  color: #333; /* Dark gray color */
  margin-bottom: 20px;
  text-align: center; /* Center align the heading */
  font-family: Playfair Display;
  font-size: 60px;
  line-height: 110px;
  font-weight: 400;
}

/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  font-size: large;
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #333;
}
/* CSS for filter buttons */
.filter-buttons {
  display: flex;
  justify-content: center;
  gap: 10px;
  margin-bottom: 20px;
}

.filter-btn {
  background-color: #040b5a;
  color: #faf0e6;
  border: none;
  padding: 10px 20px;
  border-radius: 5px;
  cursor: pointer;
  transition: background-color 0.3s ease;
}

.filter-btn:hover {
  background-color: #03346c;
}

/* CSS for card container */
.card-container {
  display: flex;
  flex-wrap: wrap;
  justify-content: center;
  gap: 20px;
}

/* CSS for individual cards */
.card {
  background-color: #faf0e6;
  border-radius: 10px;
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.1);
  overflow: hidden;
  width: calc(33.33% - 40px); /* Adjust the width according to your layout */
  max-width: 300px; /* Set a maximum width */
  margin: 20px;
  position: relative;
  transition: transform 0.3s ease;
}

.card:hover {
  transform: translateY(-10px);
}

/* CSS for card image */
.card img {
  width: 100%;
  height: auto;
  display: block;
}

/* CSS for card content */
.card-content {
  padding: 20px;
}

.card-content h3 {
  font-size: 1.2rem; /* Adjust the font size for smaller screens */
  color: #040b5a;
  margin: 0 0 10px;
}

.card-content p {
  font-size: 1rem;
  color: #333;
  line-height: 1.5;
  margin: 0 0 20px;
}

/* CSS for card button */
.card-button {
  display: none; /* Initially hidden */
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
  background-color: #040b5a;
  color: #faf0e6;
  text-decoration: none;
  padding: 10px 20px;
  border-radius: 5px;
  transition: background-color 0.3s ease;
}

.card:hover .card-button {
  display: inline-block;
}

.card-button:hover {
  background-color: #046178;
}

/* Responsive adjustments for smaller screens */
@media (max-width: 768px) {
  .filter-buttons {
    flex-wrap: wrap;
    justify-content: center;
  }

  .filter-btn {
    flex: 0 0 auto;
  }

  .card-container {
    justify-content: center; /* Center cards on smaller screens */
  }

  .card {
    width: calc(77% - 40px); /* Two cards per row */
  }

  .card-content h3 {
    font-size: 1rem; /* Adjust the font size for smaller screens */
  }
}

/* CSS for heading Projects */
.projects-heading {
  text-align: center;
  font-size: 2rem;
  color: #040b5a;
  margin-bottom: 20px;
  font-weight: bold;
  
}

```
12. **ChatGPT**: There are a few issues and suggestions for improvement in the provided CSS code:

1. **Redundant Selectors**: There are some redundant CSS selectors, such as `.next-section` being defined twice. Consolidate these duplicate selectors to improve code readability and maintainability.

2. **Unused CSS Rules**: Remove any CSS rules that are not being used in the HTML markup to reduce file size and improve performance.

3. **Missing Vendor Prefixes**: Some CSS properties like `backdrop-filter` and `border-radius` may require vendor prefixes for compatibility with older browsers. Make sure to include necessary prefixes (-webkit-, -moz-, -ms-, -o-) for these properties.

4. **Inconsistent Formatting**: Maintain consistency in formatting throughout the stylesheet, such as indentation, spacing, and line breaks, to enhance readability and maintainability.

5. **Avoid Absolute Units**: Instead of using absolute units like pixels (px) for font sizes and margins, consider using relative units like em or rem for better scalability and responsiveness.

6. **Improve Responsiveness**: Ensure that the layout is responsive across various screen sizes by testing and adjusting media queries as needed. Verify that all elements are appropriately sized and positioned on different devices.

7. **Optimize Performance**: Minimize the use of expensive CSS properties like `box-shadow` and `filter` for better rendering performance, especially on mobile devices.

8. **Accessibility Considerations**: Verify that the color contrast ratios between text and background colors meet accessibility standards, especially for users with visual impairments.

9. **Consolidate Styles**: Consolidate styles where possible by grouping similar elements and applying common properties to reduce code duplication.

10. **Semantic HTML**: Ensure that the HTML markup follows semantic conventions and uses appropriate tags for different content types, which can help with styling and accessibility.

By addressing these points, you can enhance the quality, maintainability, and performance of your CSS code. Let me know if you need further assistance with any specific aspect!
13. **You**: please fix bugs 
14. **ChatGPT**: Of course! I'd be happy to help you fix bugs. Could you please provide more details about the bugs you're encountering? What kind of program are you working on, and what seems to be the issue? The more information you provide, the better I can assist you in resolving the bugs.
15. **You**: There are a few issues and suggestions for improvement in the provided CSS code:

Redundant Selectors: There are some redundant CSS selectors, such as .next-section being defined twice. Consolidate these duplicate selectors to improve code readability and maintainability.

Unused CSS Rules: Remove any CSS rules that are not being used in the HTML markup to reduce file size and improve performance.

Missing Vendor Prefixes: Some CSS properties like backdrop-filter and border-radius may require vendor prefixes for compatibility with older browsers. Make sure to include necessary prefixes (-webkit-, -moz-, -ms-, -o-) for these properties.

Inconsistent Formatting: Maintain consistency in formatting throughout the stylesheet, such as indentation, spacing, and line breaks, to enhance readability and maintainability.

Avoid Absolute Units: Instead of using absolute units like pixels (px) for font sizes and margins, consider using relative units like em or rem for better scalability and responsiveness.

Improve Responsiveness: Ensure that the layout is responsive across various screen sizes by testing and adjusting media queries as needed. Verify that all elements are appropriately sized and positioned on different devices.

Optimize Performance: Minimize the use of expensive CSS properties like box-shadow and filter for better rendering performance, especially on mobile devices.

Accessibility Considerations: Verify that the color contrast ratios between text and background colors meet accessibility standards, especially for users with visual impairments.

Consolidate Styles: Consolidate styles where possible by grouping similar elements and applying common properties to reduce code duplication.

Semantic HTML: Ensure that the HTML markup follows semantic conventions and uses appropriate tags for different content types, which can help with styling and accessibility.

By addressing these points, you can enhance the quality, maintainability, and performance of your CSS code. Let me know if you need further assistance with any specific aspect!
16. **ChatGPT**: To address the issues and suggestions for improvement in the provided CSS code, let's break down each point and see how we can improve the CSS:

1. **Redundant Selectors:**
   - Identify duplicate selectors and consolidate them to improve code readability and maintainability. For example:
     ```css
     .next-section {
         /* styles */
     }

     .next-section {
         /* styles */
     }
     ```
     Consolidate them into a single selector:
     ```css
     .next-section {
         /* styles */
     }
     ```

2. **Unused CSS Rules:**
   - Remove any CSS rules that are not being used in the HTML markup to reduce file size and improve performance. Use tools like Chrome DevTools or browser extensions to identify unused CSS rules.

3. **Missing Vendor Prefixes:**
   - Add necessary vendor prefixes for properties like `backdrop-filter` and `border-radius` for compatibility with older browsers. For example:
     ```css
     .element {
         backdrop-filter: blur(5px);
         -webkit-backdrop-filter: blur(5px); /* Safari */
     }
     ```

4. **Inconsistent Formatting:**
   - Maintain consistency in formatting throughout the stylesheet, such as indentation, spacing, and line breaks, to enhance readability and maintainability.

5. **Avoid Absolute Units:**
   - Use relative units like `em` or `rem` instead of absolute units like `px` for font sizes and margins to improve scalability and responsiveness. For example:
     ```css
     .element {
         font-size: 1.2em;
         margin-bottom: 1.5rem;
     }
     ```

6. **Improve Responsiveness:**
   - Test and adjust media queries to ensure that the layout is responsive across various screen sizes. Verify that all elements are appropriately sized and positioned on different devices.

7. **Optimize Performance:**
   - Minimize the use of expensive CSS properties like `box-shadow` and `filter` for better rendering performance, especially on mobile devices. Use them judiciously only where necessary.

8. **Accessibility Considerations:**
   - Verify that the color contrast ratios between text and background colors meet accessibility standards. Use tools like WebAIM's Color Contrast Checker to ensure compliance.

9. **Consolidate Styles:**
   - Group similar elements and apply common properties to reduce code duplication. For example:
     ```css
     .heading {
         font-size: 1.5rem;
         font-weight: bold;
         color: #333;
     }

     .sub-heading {
         font-size: 1.2rem;
         font-weight: bold;
         color: #666;
     }
     ```
     Consolidate into:
     ```css
     .heading, .sub-heading {
         font-weight: bold;
     }

     .heading {
         font-size: 1.5rem;
         color: #333;
     }

     .sub-heading {
         font-size: 1.2rem;
         color: #666;
     }
     ```

10. **Semantic HTML:**
    - Ensure that the HTML markup follows semantic conventions and uses appropriate tags for different content types, which can help with styling and accessibility.

By addressing these points, you can enhance the quality, maintainability, and performance of your CSS code. Let me know if you need further assistance with any specific aspect!
17. **You**: new css file with fixing bug
18. **ChatGPT**: Sure, if you need to create a new CSS file to fix a bug, here's a step-by-step guide:

1. **Identify the Bug**: First, identify the bug in your current CSS file. This could be anything from layout issues to styling problems.

2. **Create a New File**: Create a new CSS file in your project directory. You can do this using any text editor or integrated development environment (IDE).

3. **Link the New File**: If you haven't already, link your new CSS file to your HTML document. You can do this by adding a `<link>` tag in the `<head>` section of your HTML file, like this:
   ```html
   <link rel="stylesheet" href="path/to/your/new-file.css">
   ```

4. **Copy Existing CSS**: Copy any CSS rules from your existing file that are not related to the bug. Paste them into your new CSS file. Make sure to maintain the same structure and formatting.

5. **Fix the Bug**: Identify the specific CSS rules causing the bug and work on fixing them. This might involve adjusting selectors, properties, or values.

6. **Test Your Changes**: After making adjustments, test your changes thoroughly to ensure they fix the bug without causing any new issues.

7. **Document Your Fixes**: It's always a good practice to document the changes you've made, especially if you're working in a team or on a collaborative project. You can add comments in your CSS file to explain the purpose of specific rules or changes.

8. **Optimize and Refactor**: Take this opportunity to review your CSS code and see if there are any other areas that could be improved or optimized. Refactor your code if necessary to make it cleaner and more efficient.

9. **Repeat Testing**: Once you're satisfied with your changes, test your website across different browsers and devices to ensure compatibility and responsiveness.

10. **Deploy**: If everything looks good, deploy your updated CSS file to your production environment.

By following these steps, you should be able to create a new CSS file and fix the bug effectively. Remember to always test your changes thoroughly and document your process for future reference.
19. **You**: make new css file for me code 
20. **ChatGPT**: Sure, I can help you create a new CSS file. Here's a basic template you can start with:

```css
/* styles.css */

/* Resetting default browser styles */
body, h1, p {
    margin: 0;
    padding: 0;
}

/* Setting default font family and size */
body {
    font-family: Arial, sans-serif;
    font-size: 16px;
}

/* Styling headings */
h1 {
    font-size: 24px;
    font-weight: bold;
    color: #333;
    margin-bottom: 20px;
}

h2 {
    font-size: 20px;
    font-weight: bold;
    color: #444;
    margin-bottom: 15px;
}

/* Styling paragraphs */
p {
    font-size: 16px;
    color: #666;
    line-height: 1.5;
    margin-bottom: 15px;
}

/* Adding some spacing utility classes */
.margin-top {
    margin-top: 15px;
}

.margin-bottom {
    margin-bottom: 15px;
}

.margin-left {
    margin-left: 15px;
}

.margin-right {
    margin-right: 15px;
}

/* Styling links */
a {
    color: #007bff;
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}

/* Styling buttons */
.button {
    display: inline-block;
    padding: 10px 20px;
    font-size: 16px;
    color: #fff;
    background-color: #007bff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
}

.button:hover {
    background-color: #0056b3;
}

/* Container */
.container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 15px;
}

/* Responsive Design */
@media (max-width: 768px) {
    body {
        font-size: 14px;
    }
    h1 {
        font-size: 20px;
    }
    h2 {
        font-size: 18px;
    }
}
```

This CSS file provides a basic starting point for styling your HTML elements. You can customize the styles according to your project's requirements. Just link this CSS file to your HTML file using the `<link>` tag inside the `<head>` section.